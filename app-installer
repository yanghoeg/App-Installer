#!/data/data/com.termux/files/usr/bin/bash
set -euo pipefail

cd "$HOME/.App-Installer" || { echo "[ERROR] App-Installer 디렉터리를 찾을 수 없습니다" >&2; exit 1; }
git pull --ff-only || { echo "[WARN] git pull 실패 — 로컬 버전으로 계속 진행" >&2; }
chmod +x install.sh
exec ./install.sh "$@"
